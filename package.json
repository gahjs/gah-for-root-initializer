{
  "name": "@awdware/gah-for-root-initializer",
  "version": "0.1.0",
  "description": "For Root Initializer",
  "main": "./lib/index.js",
  "scripts": {
    "prebuild": "del-cli lib",
    "lint": "eslint src/** --fix",
    "build": "yarn run prebuild && tsc -p .",
    "drop": "yarn run build && yarn publish --access public --no-git-tag-version "
  },
  "author": "LazyElmTree",
  "contributors": [
    "LazyElmTree",
    "LoaderB0T"
  ],
  "license": "MIT",
  "private": false,
  "dependencies": {
    "@gah/shared": "^0.6.0"
  },
  "devDependencies": {
    "@awdware/eslint-config-nodejs": "^0.0.6",
    "@types/node": "^12.0.0",
    "@typescript-eslint/eslint-plugin": "^4.13.0",
    "@typescript-eslint/parser": "^4.13.0",
    "del-cli": "^3.0.1",
    "eslint": "^7.18.0",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  }
}
